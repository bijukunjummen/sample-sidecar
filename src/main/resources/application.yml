server:
  port: 10443


eureka:
  instance:
    virtual-host-name: samplecassandra.vip
  client:
    enabled: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

spring:
  application:
    name: samplecassandra

sidecar:
  port: 9042

